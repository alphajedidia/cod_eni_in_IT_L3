import { Ionicons } from "@expo/vector-icons";
import { Colors } from "@/constants/Theme";

const CheckedIcon = () => {
  return <Ionicons size={20} name="checkmark-circle" color={Colors.green} />;
};

export default CheckedIcon;
